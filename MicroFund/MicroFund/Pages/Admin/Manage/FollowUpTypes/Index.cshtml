@page
@model MicroFund.Pages.Admin.Manage.FollowUpTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row">
        <div class="col-6">
            <h2>Follow Up Types</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-page="Create" class="btn btn-primary"><i class="fas fa-plus"></i>Create New</a>
        </div>
    </div>
    <table class="table" id="followUpTypesTable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FollowUpType[0].FollowUpTypeDescription)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FollowUpType[0].IsArchived)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.FollowUpType)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FollowUpTypeDescription)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IsArchived)
                    </td>
                    <td>
                        <a class="btn btn-outline-success" asp-page="./Edit" asp-route-id="@item.FollowUpTypeId">Edit</a> |
                        <a class="btn btn btn-outline-warning" asp-page="./Details" asp-route-id="@item.FollowUpTypeId">Details</a> |
                        <a class="btn btn-outline-danger" asp-page="./Delete" asp-route-id="@item.FollowUpTypeId">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts{
    <script>
        $(document).ready(function () {
            $('#followUpTypesTable').DataTable({
                "pageLength": 5,
                "order": [[2, "asc"]]
            });
            $('.dataTables_length').addClass('bs-select');
        });
    </script>
}