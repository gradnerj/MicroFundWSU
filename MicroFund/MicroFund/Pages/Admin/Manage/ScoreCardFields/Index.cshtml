@page
@model MicroFund.Pages.Admin.Manage.ScoreCardFields.IndexModel

@{
    ViewData["Title"] = "Index";
    ViewData["Dashboard"] = "Admin Dashboard";
}


<div class="container">
    <div class="row">
        <div class="col-6">
            <h2>Score Card Fields</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-page="Create" class="btn btn-primary"><i class="fas fa-plus"></i>Create New</a>
        </div>
    </div>
    <table class="table" id="scoreCardFieldsTable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ScoreCardField[0].ScoreCardFieldDescription)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ScoreCardField[0].ScoringCategory)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ScoreCardField[0].Weight)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ScoreCardField[0].IsArchived)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.ScoreCardField)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ScoreCardFieldDescription)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ScoringCategory.ScoringCategoryDescription)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Weight)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IsArchived)
                    </td>
                    <td>
                        <a class="btn btn-outline-success" asp-page="./Edit" asp-route-id="@item.ScoreCardFieldId">Edit</a> |
                        <a class="btn btn btn-outline-warning" asp-page="./Details" asp-route-id="@item.ScoreCardFieldId">Details</a> |
                        <a class="btn btn-outline-danger" asp-page="./Delete" asp-route-id="@item.ScoreCardFieldId">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts{
    <script>
        $(document).ready(function () {
            $('#scoreCardFieldsTable').DataTable({
                "pageLength": 5,
                "order": [[2, "asc"]]
            });
            $('.dataTables_length').addClass('bs-select');
        });
    </script>
}